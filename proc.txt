1. create an git repo and clone it to ur local system open in vs 

2. created experiment folder - > inside this get ur prject file and the dataset

3.  create new folder named src -> add first component file i.e data_ingestion 

4. run data_ingestion ull see log folder that tracks all the steps in a defined format then 
-> data folder that has the train and test csv files 

5. run data_preprocessing.py file a sub folder in data as interim that has the processed data
 of train and test set and a log file that has the info abt the process taken place  

6. run feature_engineering.py file this creates a subfolder in data called processed that contains the train and test set after applyin tf-idf vectorizer

7. run model_training.py file lly model_evaluation then  ull get the reports folder that has all the performance metrics

8. add data models reports dir to .gitignore as it can even become larger further and we dont need git to track 



set up dvc pipeline ------>>>>
                              to automate the entire pipeline using dvc 
                              --intialize the dvc then 
                              --create an dvc.yaml file add config 
                              --then run the dvc repro that runs all the artifacts metioned in the config file 
                              --this creates dvc.lock file that has all the recrds abt the artifacts alsi the hash - contains the type of hash (here md5) 
                               and md5 has actual value
                               evry artifactr has its own hash value
                               -- it suggests u to add the dvc,lock foleder tto git so that when u rollback for an older version of the code u can get the data used in the particular code using the hash value dvc provides this..


ste up dvc pipeline with params ------->>>>>
                               create a params.yaml file (industry practice)
                              -- after setting params
                              -- do dvc repro


# PARAMS setup
# ------------------------------------------------------------

# params.yaml setup:
# 1> import yaml
# 2> add func:

def load_params(params_path: str) -> dict:
    """Load parameters from a YAML file."""
    try:
        with open(params_path, 'r') as file:
            params = yaml.safe_load(file)
        logger.debug('Parameters retrieved from %s', params_path)
        return params

    except FileNotFoundError:
        logger.error('File not found: %s', params_path)
        raise

    except yaml.YAMLError as e:
        logger.error('YAML error: %s', e)
        raise

    except Exception as e:
        logger.error('Unexpected error: %s', e)
        raise


# ------------------------------------------------------------
# Add to main():

# data_ingestion
params = load_params(params_path='params.yaml')
test_size = params['data_ingestion']['test_size']

# feature_engineering
params = load_params(params_path='params.yaml')
max_features = params['feature_engineering']['max_features']

# model_building
params = load_params('params.yaml')['model_building']
train_data = load_data('./data/processed/train_tfidf.csv')